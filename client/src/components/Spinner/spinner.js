import React from 'react';
import './spinner.css'; // You can define your own CSS for the spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
